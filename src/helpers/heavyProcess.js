export const heavyProcess = (iterations) => {
    for (let index = 0; index < iterations; index++) {
        console.log("A mi hay que darme banda de 🚚🚚🚚");
    }

    return `realized iterations: ${iterations}`
}